#include <stdio.h>

int main() {
    double montante;
    int totalCentimos, i, quantidade, moedas;
    char nomes;

    printf("Introduza um montante em euros, podendo ter centimos: ");
    scanf("%f", &montante);
}