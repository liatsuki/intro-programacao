#include <stdio.h>
// Ciclo | Exercicio 16 | Soma dos primeiros 4 quadrados naturais

int main() {
    int soma = 0;

    soma = soma + 1 * 1;
    soma = soma + 2 * 2;
    soma = soma + 3 * 3;
    soma = soma + 4 * 4;

    printf("Soma dos primeiros 4 quadrados: %d", soma);
}